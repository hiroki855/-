# import_pleasanter.py コード解説（Python初心者向け）

このドキュメントは、`import_pleasanter.py` を読みながら Python の基礎を学べるように、ブロックごとに「何をしているか」「なぜそうするか」を解説します。

---

## 1. ファイル冒頭の `# -*- coding: utf-8 -*-`

Pythonファイルの文字コードを宣言する古い書き方です。Python3 では UTF-8 がデフォルトなので必須ではありませんが、Windows環境で日本語が混ざる場合の保険として書いています。

---

## 2. import文

```python
import csv
import datetime
...
import requests
```

- **`import X`** … 標準ライブラリや外部ライブラリを「読み込んで使えるようにする」宣言。Pythonの世界では「機能ごとにモジュールがあり、必要なものを取り込んで使う」のが基本。
- **標準ライブラリ**（csv, datetime, os, sys, time, json, traceback）はPythonに最初から入っているのでインストール不要。
- **外部ライブラリ**（requests）は `pip install requests` でインストールが必要。

---

## 3. 設定エリア（定数）

```python
HOST_URL = "###REPLACE_ME###"
API_KEY = "###REPLACE_ME###"
SITE_ID = 44884
```

Python には他言語のような「定数（const）」キーワードはありませんが、**全部大文字の変数名は「書き換えない値」というのが慣習**です。
コード冒頭にまとめておくと、運用前に1箇所だけ書き換えれば済みます。

### r"…" の意味（raw文字列）

```python
CSV_DIR = r"C:\Users\hn125141\Desktop"
```

`r` を先頭に付けると「エスケープを解釈しない文字列」になります。Windowsのパスには `\` が多いので、`r` を付けないと `\U`, `\n` などが特殊文字扱いされてしまいます。

---

## 4. COLUMN_MAPPING（マッピング定義）

```python
COLUMN_MAPPING = [
    {"csv": "申込番号", "code": "ClassA", "hash": "ClassHash", "type": "str", "required": True},
    ...
]
```

- `[ ... ]` … **リスト**（順序付きの配列）
- `{ "key": "value", ... }` … **辞書（dict）**（キーと値のペアの集合）

11個の辞書を持つリストで「CSVのこの列はプリザンターのこの項目に対応する」という対応表を表しています。

### なぜ `hash` を持たせているか？

プリザンターAPIは、項目種別ごとにまとめたグループ（ハッシュ）として送る仕様です。

```json
{
  "Title": "山田太郎",
  "ClassHash":       { "ClassA": "A001", "ClassB": "ヤマダ" },
  "DateHash":        { "DateA": "2026/05/15" },
  "NumHash":         { "NumA": 1, "NumB": 1000 },
  "DescriptionHash": { "DescriptionA": "備考メモ" }
}
```

なので、各項目が「どのハッシュに入るか」をマッピング表に持たせています。
タイトルだけは特別で、ルート直下に置くため `hash: None`（None＝値が無いことを表すPythonの特殊値）にしています。

---

## 5. 関数定義 `def`

```python
def log(message: str) -> None:
    ...
```

- `def` … 関数を定義するキーワード
- `message: str` … 引数 `message` の **型ヒント**。「文字列を受け取る」ことを表す注釈で、実行時に強制はされませんがコードの可読性とエディタの補完に役立ちます
- `-> None` … 戻り値の型ヒント。None＝何も返さない関数

### f文字列

```python
line = f"[{timestamp}] {message}"
```

`f"..."` の中に `{変数名}` と書くと、その変数の値が埋め込まれます。文字列連結よりも読みやすく、現代Pythonの標準的な書き方です。

---

## 6. 例外処理 try / except

```python
try:
    # 失敗する可能性のある処理
    ...
except requests.RequestException as e:
    # 通信エラー時の処理
    ...
except Exception:
    # それ以外の想定外エラー
    ...
```

- **try** … 「危険な処理」を囲む
- **except** … エラーが起きたときの処理を書く
- `as e` … 例外オブジェクトを変数 `e` に受け取る（中にエラー内容が入っている）

Python の例外は階層構造になっていて、`requests.RequestException` のような**具体的なもの**を先に書き、`Exception`（一番広い）を最後に書くのが定石です。

### `continue` の意味

```python
continue  # 次の行へ
```

ループの中で `continue` を呼ぶと、**残りの処理をスキップして次の繰り返しに進みます**。「この行はダメだったから次の行へ」というときに使います。

---

## 7. ファイル操作 with open

```python
with open(csv_path, "r", encoding="utf-8", newline="") as f:
    reader = csv.DictReader(f)
    for line_no, row in enumerate(reader, start=2):
        ...
```

- `with open(...) as f:` … ファイルを開いて `f` という変数に入れる。`with` ブロックを抜けると**自動でファイルを閉じてくれる**ので閉じ忘れの心配がない
- `"r"` … 読み込みモード（write なら `"w"`、追記なら `"a"`）
- `csv.DictReader(f)` … 1行目をヘッダーとして読み、2行目以降を「ヘッダー名→値」の辞書で返してくれる
- `enumerate(reader, start=2)` … ループに番号を付ける。`start=2` でデータの実際の行番号（ヘッダーが1行目なので2から）

---

## 8. 辞書の便利メソッド

### `dict.get(key, default)`

```python
raw = row.get(m["csv"], "")
```

「キーがあればその値、なければデフォルト値」を返す。`row[m["csv"]]` だとキーが無いときにエラーになるので、安全側に倒すために使います。

### `dict.setdefault(key, default)`

```python
payload.setdefault(m["hash"], {})[m["code"]] = value
```

「キーがなければデフォルト値で作って返す」。`ClassHash` のキーが無ければ空辞書 `{}` を作り、`ClassA: value` を入れる、という動作を1行で書いています。

---

## 9. requests でAPIを叩く

```python
response = requests.post(url, json=body, timeout=REQUEST_TIMEOUT)
response.raise_for_status()
return response.json()
```

- `requests.post(url, json=body, ...)` … 引数 `json=` に辞書を渡すと、自動でJSON化＆ヘッダ設定をしてくれる
- `raise_for_status()` … HTTPステータスが4xx/5xx（エラー）なら例外を発生させる
- `response.json()` … 戻り値の本文をJSONとして辞書に変換

---

## 10. `if __name__ == "__main__":`

```python
if __name__ == "__main__":
    sys.exit(main())
```

Python の定型句。「このファイルが**直接実行されたとき**だけ `main()` を呼ぶ」という意味です。
`import import_pleasanter` のように他から読み込まれたときは実行されないので、ライブラリとしても使い回せる安全な書き方です。

`sys.exit(0)` … 終了コード0（正常）でプログラムを終了。`.bat` 側で成否判定する場合に使えます。

---

## 11. 全体の流れの再確認

```
[実行] run_import.bat ダブルクリック
   ↓
[Python起動] import_pleasanter.py
   ↓
① 今日の日付からCSVパスを組み立て
② ファイル存在チェック（無ければ終了）
③ CSVを1行ずつ辞書として読む
④ 各行：
   a. 必須チェック（NGなら失敗ログ＋次の行）
   b. プリザンターに「申込番号」で問い合わせ
   c. 既存あり → 更新API（ログに「上書きしました」）
      既存なし → 作成API（ログに「新規登録」）
   d. 通信エラー → 失敗ログ＋次の行
⑤ サマリ表示（新規/上書き/失敗の件数）
   ↓
[pause で待機] 確認後にウィンドウを閉じる
```

---

## 12. 運用前の置き換えチェックリスト

1. `HOST_URL` … プリザンターのURL（末尾`/`なし）
2. `API_KEY` … プリザンターで発行したAPIキー
3. `SITE_ID` … 取込先サイトID（要件では 44884）
4. `COLUMN_MAPPING` … 実際の項目コードに合わせて見直し
5. `CSV_DIR` … デスクトップのパス（ユーザー名が hn125141 以外なら変更）

---

## 13. テストの進め方（推奨）

1. **APIキー誤り** … わざと違う値にして実行。「API通信エラー: 401 Unauthorized」のような失敗ログが出ればOK
2. **CSVなし** … ファイルを置かずに実行。「CSVファイルが見つかりません」で終了すればOK
3. **必須欠落** … 申込番号など必須列を空にした行を含めて実行。「必須項目欠落」ログが出てその行はスキップされるか確認
4. **新規登録** … 一度プリザンター側を空にして実行。新規件数 = CSV件数 になるか確認
5. **上書き** … 同じCSVをもう一度実行。上書き件数 = CSV件数 になるか確認

---

困ったら `import_YYYYMMDD_log.txt` を見ると、どの行で何が起きたかが日本語で残ります。
