# プリザンター 住所処理スクリプト 要件定義書 (v2)

## 1. 背景・目的

プリザンター上で顧客情報等を管理する際、住所入力の手間と表記ゆれを削減したい。
本要件では、ユーザーが住所欄に自由入力した文字列から、以下を自動で導出する。

- 郵便番号
- 都道府県／市区町村／町域＋丁目／その他（番地・建物名）の4区分

これらをひとつのスクリプト群（JavaScript）として実装する。

---

## 2. 採用API

### 2.1 PostcodeJP API（住所解析API）

| 項目 | 内容 |
| --- | --- |
| 提供元 | 株式会社プラスゼロ（PostcodeJP API） |
| エンドポイント | `POST https://apis.postcode-jp.com/api/v6/parse` |
| 認証 | APIキー（HTTPヘッダ `apikey` または クエリパラメータ `apiKey`） |
| CORS | 対応済み（ブラウザJSから直接呼び出し可） |
| プラン | 無料プラン（Free: 384回/日・1回/秒。回数は不定期に変動するとの注記あり）からスタート、必要に応じて有償プランへ |
| 注意点 | **本APIはプレビュー版**。仕様変更の可能性があるため、本番投入前にPostcodeJP社の利用規約とリリースノートを確認すること |

### 2.2 入出力サマリ
- リクエストボディ: `{ "address": "東京都杉並区阿佐谷南三丁目1-2 サンプルマンション101" }`
- レスポンス: `japanese`（構造化住所）、`meta.postal_code`（郵便番号配列）、`meta.score`（信頼度）

### 2.3 代替案（将来検討）
プレビュー版が業務利用に不向きと判断された場合、`/api/v6/postcodes` の一覧APIをフィルタ構文で呼び出し、住所分割はクライアント側で正規表現実装する案に切り替える。

---

## 3. 対象環境

| 項目 | 内容 |
| --- | --- |
| プラットフォーム | プリザンター（Pleasanter） |
| スクリプト種別 | **画面スクリプト**（クライアント側 JavaScript） |
| 実行コンテキスト | 編集画面（新規作成・編集の両方） |
| 通信方式 | `fetch` API |
| APIキーの管理 | プリザンターのスクリプト内に定数として埋め込む（**HTTPリファラ制限をPostcodeJP管理画面で設定し、不正使用を防ぐ**） |
| 対象サイトID | **44884** |

> **APIキーの取扱注意**
> 画面スクリプトはブラウザに配信されるため、APIキーは公開されます。必ずPostcodeJPダッシュボードで「HTTPリファラ制限」を設定し、自社プリザンターのURLからのみ使えるよう保護すること。

### 3.1 APIキー管理ポリシー

PostcodeJPは1アカウントで複数のAPIキーを発行できる。本プロジェクトでは**環境ごとにキーを分離**して運用する。

#### 3.1.1 キー分離方針

| 環境 | キー用途 | 想定リファラ |
| --- | --- | --- |
| 開発環境 | 開発者ローカル・ステージング検証用 | `http://localhost/*`, 検証プリザンターのURL |
| 本番環境 | 本番プリザンターからのみ呼び出し | 本番プリザンターのURL |

理由：
- 開発キーが漏洩しても本番リクエスト枠（Freeプラン384回/日）を消費されない
- 開発時の動作確認による意図せぬAPI呼び出し数増加で、本番運用に影響を与えない
- 万一のキー失効・ローテーション時、影響範囲を環境単位に限定できる

#### 3.1.2 キー命名規則

PostcodeJPダッシュボードでキーを作成する際の名称（ラベル）は以下の形式とする：

```
pleasanter-{サイトID}-{環境}
```

具体例：
- `pleasanter-44884-dev`
- `pleasanter-44884-prod`

複数サイトに本スクリプトを横展開する場合も、サイトIDを含めることで一覧で識別可能になる。

#### 3.1.3 リファラ制限設定手順（PostcodeJPダッシュボード）

1. https://console.postcode-jp.com にログイン
2. 「APIキーを作成」をクリックし、上記命名規則に従ってキー名を入力
3. 生成されたキーの「制限設定」→「HTTPリファラ」を選択
4. 許可するリファラを登録：
   - 開発キー：`http://localhost/*`、検証環境URL（例：`https://pleasanter-stg.example.co.jp/*`）
   - 本番キー：本番URL（例：`https://pleasanter.example.co.jp/*`）
5. 設定を保存
6. キー値をコピーし、安全な経路（社内パスワード管理ツールなど）でスクリプト管理者に共有

#### 3.1.4 スクリプトへの埋め込み方針

- `config.js` 内の `API_KEY` 定数として配置（セクション14.1の雛形参照）
- **本番デプロイ時は本番キーに差し替える**運用フローをREADMEに明記
- 開発キー・本番キーをコードリポジトリに直接コミットしない（`config.js` を `.gitignore` 対象とし、`config.sample.js` をテンプレートとして配布する形式を推奨）

#### 3.1.5 キーローテーション

漏洩疑い・退職者発生・定期見直しのいずれかが発生した場合、以下の手順でキーを更新する：

1. PostcodeJPダッシュボードで新しいキーを作成
2. `config.js` の `API_KEY` を新キーに差し替え、デプロイ
3. 動作確認後、旧キーをダッシュボードから削除

---

## 4. 項目定義（フィールド設計）

プリザンターの分類項目に以下を割り当てる。スクリプト先頭の定数で集中管理し、後日サイト間で再利用する際にここだけ書き換えれば動くようにすること。

| 論理名 | 物理名 | 型 | 用途 |
| --- | --- | --- | --- |
| 住所（全体） | **ClassH** | 分類H | ユーザーの自由入力欄。本スクリプトの入力元（onChangeトリガー対象） |
| 郵便番号 | **ClassN** | 分類N | API応答 `meta.postal_code[0]` を `XXX-XXXX` 形式でセット |
| 都道府県 | **ClassY** | 分類Y | `japanese.prefecture` |
| 市区群 | **ClassX** | 分類X | `japanese.county + japanese.city + japanese.ward`（郡・市・区・政令市の行政区を連結） |
| 丁目 | **ClassS** | 分類S | `japanese.district + japanese.chome`（町域名と丁目を連結） |
| 番地～建物名 | **ClassQ** | 分類Q | `japanese.block + japanese.house_num + japanese.building + japanese.room`（番地・建物名・部屋番号を連結） |

### 4.1 フィールド連結ルール

API応答は要素が細かく分かれて返るため、上記4区分への集約ルールを明示しておく。

| 出力フィールド | 連結する API レスポンス要素 | 区切り |
| --- | --- | --- |
| ClassY（都道府県） | `prefecture` | なし |
| ClassX（市区群） | `county` + `city` + `ward` | なし（直接連結） |
| ClassS（丁目） | `district` + `chome` | なし（直接連結） |
| ClassQ（番地～建物名） | `block` + `-` + `house_num` + 半角スペース + `building` + 半角スペース + `room` | 番地はハイフン区切り、建物名と部屋番号は半角スペース区切り |

- 各要素が空文字や null の場合は、その要素と区切り文字をスキップする（末尾に不要なスペースやハイフンを残さない）
- 最終的に各フィールドへセットする前に trim 処理を行うこと

---

## 5. 機能要件

### 5.1 トリガー
- 住所欄（**ClassH**）の `onChange`
- 推奨：デバウンス 500ms（連続入力時のAPI呼び出し抑止）

### 5.2 処理フロー

```
[住所欄onChange]
      ↓
[入力値が空 → 関連項目クリアして終了]
      ↓
[入力値を正規化（全角半角、空白圧縮、トリム）]
      ↓
[PostcodeJP /parse をPOST]
      ↓
[レスポンス判定]
      ├─ 成功 & 単一郵便番号 → 各フィールドへ自動セット
      ├─ 成功 & 複数郵便番号 → ユーザー選択UIを表示
      ├─ 成功だがスコア低（<0.7） → 警告表示しつつ低信頼でセット
      └─ 失敗 → エラー通知、フィールドは触らない
```

### 5.3 複数候補の選択UI仕様（ドロップダウン形式）

`meta.postal_code` が2件以上の場合、ユーザーに選ばせる。

#### UI要件
- **表示形式**：住所欄（ClassH）の直下にドロップダウンリストを動的に挿入する
- **配置**：ClassH 入力欄の DOM 直下（次の兄弟要素）に挿入し、入力欄と同じ幅で揃える
- **表示内容**：各候補1行に「郵便番号（XXX-XXXX形式）」「半角スペース」「住所全文」の順で記載
  - 例：`166-0004 東京都杉並区阿佐谷南`
  - 例：`160-0001 東京都新宿区片町`
- **インタラクション**：
  - 候補行にマウスホバーで背景色を変更（視認性確保）
  - 候補をクリックすると、その郵便番号と住所要素を各フィールド（ClassN/Y/X/S/Q）にセットし、ドロップダウンを閉じる
  - ドロップダウン外をクリックしても閉じる（何もセットしない）
  - Esc キーでも閉じる
- **件数制御**：
  - 候補が **5件以下** → そのまま全件表示
  - 候補が **5件を超える場合** → 先頭5件を表示し、リスト最上部に「該当が多すぎます。住所をより具体的に入力してください」という警告メッセージを赤字で表示
- **多重表示防止**：ドロップダウンが既に表示中の状態で新しいAPI応答が来た場合、既存ドロップダウンを破棄してから新しいものを描画する

#### スタイル要件
- 背景色：白
- 枠線：1px solid #ccc
- 各行の高さ：32px 程度（クリックしやすい）
- フォントサイズ：プリザンターの既定値に揃える
- z-index：プリザンターの他UI要素より前面に出るよう十分高い値を設定（推奨：1000以上）
- 影：軽い drop-shadow を付与してフローティング感を出す

#### 実装上の判断ポイント（Claude Codeに任せる箇所）
- DOM 構築は jQuery（プリザンター同梱版）でもバニラJSでも可
- プリザンターの既存UI要素のクラス名と衝突しないよう、独自プレフィックス（例：`addr-resolver-dropdown`）を付ける

### 5.4 郵便番号のフォーマット
- セット時は **`123-4567` のハイフン区切り** 形式に変換
- API応答 `meta.postal_code` は配列。`["1660004"]` のような7桁数字文字列

### 5.5 信頼度スコアによる挙動

| `meta.score` | 挙動 |
| --- | --- |
| 0.9以上 | 通常セット |
| 0.7以上0.9未満 | セットするが、画面上に「住所が一部推測されています。ご確認ください」を表示 |
| 0.7未満 | セットせず、「住所を正確に再入力してください」を表示 |

### 5.6 入力値の正規化
スクリプト側で事前に実施（API側でも吸収されるが、二重化で安定性を上げる）:
- 全角英数字 → 半角
- 全角スペース → 半角スペース
- 連続スペースを1つに圧縮
- 前後トリム
- 改行・タブを削除

---

## 6. エラーハンドリング

| ケース | HTTPステータス | 挙動 |
| --- | --- | --- |
| 入力空 | （呼ばない） | 関連フィールドをクリア |
| API認証エラー | 401 | コンソールエラー、ユーザーには「住所検索に失敗しました（管理者へ連絡）」 |
| レート制限 | 429 | 「アクセスが集中しています。少し待ってから再入力してください」を表示。**自動リトライしない** |
| 解析失敗（住所不正） | 400 | 「住所が正しく解析できませんでした」を表示、フィールドは触らない |
| サーバエラー | 500/503 | 「住所検索サービスが一時停止しています」を表示 |
| ネットワーク失敗 | （fetch reject） | 同上 |
| タイムアウト | （5秒） | 「住所検索がタイムアウトしました」を表示 |

- 同じ住所値に対するAPI呼び出しはセッション内でキャッシュし、無駄な呼び出しを防ぐ
- レート制限ヘッダ（`x-ratelimit-remaining-day`）を監視し、残数が10を切ったらコンソールに警告ログ

---

## 7. 競合制御

- 住所欄の更新 → 自動で各フィールドが書き換わる
- 逆方向（各フィールドを手動編集 → 住所欄に集約）は **本フェーズではスコープ外**
- ユーザーが各分割フィールドを手動で編集した後に住所欄を再編集した場合、**住所欄起点で全フィールドが上書きされる**ことを利用ガイドに明記

---

## 8. 非機能要件

| 項目 | 要件 |
| --- | --- |
| レスポンス時間 | API応答含めて2秒以内を目標 |
| デバウンス | 500ms |
| 同住所キャッシュ | セッション内で同一住所文字列のAPI結果を保持 |
| ログ | `console.debug` 使用（本番ビルドで抑制可能な前提） |
| ブラウザ対応 | プリザンター動作保証ブラウザ（Chrome, Edge 最新） |
| プリザンター本体 | バージョンは要確認（`$p.events.on` などのスクリプトAPI互換性のため） |

---

## 9. 制約事項

- プリザンターの画面スクリプトAPI（`$p.getControl`, `$p.set`, `$p.events` 等）の範囲内で実装
- 既存項目の変更は行わない
- jQuery はプリザンター同梱版に依存
- PostcodeJP APIはHTTPS必須
- **APIキーがブラウザに露出する前提**でリファラ制限を必ず併用

---

## 10. テストケース

### 10.1 正常系

各行の「期待される分割」は `ClassY / ClassX / ClassS / ClassQ` の順で記載。

| No | 入力（ClassHへ入力） | 期待 ClassN（郵便番号） | 期待 ClassY / ClassX / ClassS / ClassQ |
| --- | --- | --- | --- |
| 1 | `東京都杉並区阿佐谷南三丁目1-2` | 166-0004 | 東京都/杉並区/阿佐谷南三丁目/1-2 |
| 2 | `神奈川県横浜市西区みなとみらい2-3-5` | 220-0012 | 神奈川県/横浜市西区/みなとみらい/2-3-5 |
| 3 | `北海道札幌市中央区北一条西二丁目` | 060-0001 | 北海道/札幌市中央区/北一条西二丁目/（空） |
| 4 | `沖縄県国頭郡恩納村字山田1234` | 904-0411 | 沖縄県/国頭郡恩納村/字山田/1234 |
| 5 | `京都府京都市東山区祇園町南側570-2` | 605-0074 | 京都府/京都市東山区/祇園町南側/570-2 |
| 6 | `東京都新宿区市谷本村町3-23 グラネステート9階205号室` | 162-0845 | 東京都/新宿区/市谷本村町/3-23 グラネステート9階 205号室 |
| 7 | 全角入力 `東京都杉並区阿佐谷南三丁目１ー２` | 166-0004 | 同No.1 |

### 10.2 複数候補ケース
- `東京都港区芝浦` → 港区芝浦は丁目によって郵便番号が分かれるため、複数候補が返る想定。**選択UIが表示されることを確認**

### 10.3 異常系

| No | 入力 | 期待挙動 |
| --- | --- | --- |
| 1 | （空文字） | 関連フィールド全クリア |
| 2 | `あいうえお` | スコア低、警告表示、フィールド更新せず |
| 3 | `東京都××区` | スコア低、警告表示 |
| 4 | （ネットワーク切断） | エラー通知、フィールド維持 |
| 5 | （APIキー無効） | エラー通知 |

### 10.4 性能テスト
- 同一住所を続けて10回入力 → API呼び出しはキャッシュにより1回のみ
- 異なる住所を300ms間隔で5回入力 → デバウンスにより最後の1回のみAPI呼び出し

---

## 11. 成果物

### 11.1 ファイル構成

| ファイル名 | 内容 | 配置 |
| --- | --- | --- |
| `address-resolver.js` | メインスクリプト（onChangeハンドラ、API呼び出し、フィールドセット） | プリザンター「スクリプト」機能に登録 |
| `address-resolver-utils.js` | 正規化、郵便番号フォーマット変換、キャッシュ管理などのユーティリティ | 同上 |
| `address-resolver-ui.js` | 複数候補選択UI、警告メッセージ表示 | 同上 |
| `config.js` | APIキー、フィールド物理名、閾値などの設定 | 同上、**Git管理外推奨** |
| `README.md` | セットアップ手順、テスト手順、利用上の注意 | リポジトリ同梱 |

### 11.2 コーディング規約
- ES6+ 構文（`const`/`let`、アロー関数、テンプレートリテラル、async/await）
- `"use strict";` 宣言
- 命名規則：関数・変数はキャメルケース、定数はSCREAMING_SNAKE_CASE
- JSDoc コメントで関数の入出力を記載
- ESLint推奨設定（airbnb-base 程度）に準拠

### 11.3 ドキュメント
README.mdには以下を含めること:
- 前提条件（プリザンターのバージョン、PostcodeJPアカウント要否）
- APIキーの取得手順
- スクリプトのプリザンターへの登録手順
- 動作確認用テストデータ
- トラブルシューティング（よくあるエラーと対処）

---

## 12. 今後の拡張（参考、本フェーズでは実装しない）

- 各分割フィールドを手動編集した結果を住所欄へ集約する逆方向同期
- 過去入力住所のサジェスト機能
- 国際住所対応（ローマ字表記の活用）
- サーバスクリプト（プリザンター）との二重チェックによるバリデーション強化

---

## 13. 確認事項（実装前にHiroが決めるもの）

- [x] 対象サイトID：**44884**
- [x] 各フィールドの物理名：住所=ClassH / 郵便番号=ClassN / 都道府県=ClassY / 市区群=ClassX / 丁目=ClassS / 番地～建物名=ClassQ
- [ ] PostcodeJPアカウントの作成と無料プラン（384回/日）で要件充足するか試算（不足時の有料プラン候補も検討：Small 1,200回/日、Mid 3,400回/日など）
- [ ] APIキーを **開発用・本番用の2本** 取得し、それぞれにリファラ制限を設定（3.1節参照）
- [ ] `config.js` を `.gitignore` 登録、`config.sample.js` を作成してリポジトリ管理
- [ ] PostcodeJPプレビュー版APIを業務利用してよいか、上長・客先に確認
- [ ] エラーメッセージの文言を社内表現に合わせる
- [x] 複数候補選択UI：**ドロップダウン形式**（住所欄直下にインライン展開）

---

## 14. Claude Codeへの実装指示（要点）

このセクションはClaude Codeに渡す際の指示書として使う。

1. **API仕様の事実確認を最優先で行うこと**。本要件書は `2026-05-12` 時点でPostcodeJP公式ドキュメント（https://api-doc.postcode-jp.com/）を参照して作成したが、プレビュー版APIのため仕様が変わっている可能性がある。実装前に最新リファレンスを確認すること
2. プリザンターのスクリプトAPI（`$p.events.on`, `$p.getControl`, `$p.set` など）はバージョンによって挙動が異なる。**動作確認環境のプリザンターバージョンを確認**してから書き始めること
3. APIキーをコード内にハードコードしない。`config.js` に分離し、README.md に「実環境で書き換える」と明記する
4. テストケース11.3の異常系を必ずカバーすること。エラーハンドリングの抜けは本案件で最も避けたいリスク
5. デバウンスとキャッシュは性能とAPI制限の両面で必須。手抜きしないこと
6. **不明点があれば実装前に質問すること**。仕様の勝手な解釈で進めない

### 14.1 定数定義の雛形

`config.js` の先頭に以下を必ず置くこと（値はそのまま採用）:

```javascript
"use strict";

const ADDRESS_RESOLVER_CONFIG = {
  // PostcodeJP
  API_ENDPOINT: "https://apis.postcode-jp.com/api/v6/parse",
  API_KEY: "（PostcodeJPダッシュボードで取得して書き換える）",

  // 対象サイト
  SITE_ID: 44884,

  // プリザンター フィールド物理名
  FIELD: {
    ADDRESS_FULL: "ClassH",   // 住所（全体・入力欄）
    POSTAL_CODE:  "ClassN",   // 郵便番号
    PREFECTURE:   "ClassY",   // 都道府県
    CITY:         "ClassX",   // 市区群
    DISTRICT:     "ClassS",   // 丁目
    REST:         "ClassQ",   // 番地～建物名
  },

  // 挙動チューニング
  DEBOUNCE_MS: 500,
  REQUEST_TIMEOUT_MS: 5000,
  SCORE_THRESHOLD_OK: 0.9,
  SCORE_THRESHOLD_WARN: 0.7,
  MAX_CANDIDATES_BEFORE_WARN: 5,
};
```
